# 神韵·内容板块 - 完整图表文档

> 从用户意图到AI生成，从热点发现到爆款内容
> ——— 2026年3月4日 ——— v1.0

---

## 📋 目录

1. [整体架构图](#整体架构图)
2. [工作流程图](#工作流程图)
3. [页面框架图](#页面框架图)
4. [组件结构图](#组件结构图)
5. [数据流图](#数据流图)
6. [模式切换逻辑](#模式切换逻辑)
7. [API调用图](#api调用图)

---

## 一、整体架构图

### 1.1 三层架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                    神韵·内容板块                            │
│              (Content Factory - Soul Rhythm)                    │
└─────────────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────────┼─────────────────────────┐
        │                     │                     │
        ↓                     ↓                     ↓
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│  前端展示层     │   │  业务逻辑层     │   │  AI 生成层     │
│  (Frontend)    │   │  (Business)     │   │  (AI Engine)   │
├───────────────┤   ├───────────────┤   ├───────────────┤
│ React +        │   │ 路由 + 状态     │   │ Dify          │
│ Tailwind CSS   │   │ 管理           │   │ Chatflow      │
│               │   │               │   │               │
│ • 用户界面     │   │ • 模式切换     │   │ • 热点分析     │
│ • 交互反馈     │   │ • 数据传递     │   │ • 内容生成     │
│ • 流式渲染     │   │ • 错误处理     │   │ • 深度洞察     │
└───────────────┘   └───────────────┘   └───────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              ↓
                    ┌───────────────────┐
                    │  数据持久层      │
                    │  (Data Layer)   │
                    ├───────────────────┤
                    │ • Supabase      │
                    │ • 策略上下文     │
                    │ • 历史记录       │
                    └───────────────────┘
```

### 1.2 核心理念

```
极简设计 + 流式输出 + 智能生成 = 神韵

设计原则：
  ✅ 清晰 > 复杂
  ✅ 功能 > 美观
  ✅ 一致 > 独特

交互特点：
  ✅ 实时反馈
  ✅ 流式输出
  ✅ 错误友好
```

---

## 二、工作流程图

### 2.1 完整用户流程

```
┌─────────────────────────────────────────────────────────────────────┐
│  用户进入神韵·内容板块                                        │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  选择工作模式（左侧栏导航）                │
        │  ┌───────────────────────────────────┐      │
        │  │ 1. 找爆款选题 🔍              │      │
        │  │ 2. 内容创作 ✍️               │      │
        │  │ 3. 数据洞察 📊                │      │
        │  │ 4. 发布计划 📅                 │      │
        │  └───────────────────────────────────┘      │
        └───────────────────────────────────────────────┘
                              ↓
                    ┌─────────────────────────┐
                    │  模式A：找爆款选题     │
                    └─────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤1：输入搜索关键词                     │
        │  [AI、创业、内容营销...]               │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤2：获取实时热点                     │
        │  GET /api/news-radar?keyword=xxx         │
        │  ────────────────────────────────────         │
        │  ✅ ai-news-radar 数据源                │
        │  • 聚合 10+ 科技/AI 资讯网站              │
        │  • 每 30 分钟自动更新                     │
        │  • 潜力打分算法                          │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤3：查看热点文章列表                 │
        │  • 标题 + 来源 + 时间                    │
        │  • 潜力分（0-100）                     │
        │  • 相关标签                            │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤4：查看AI选题建议                 │
        │  • 基于热点生成选题方向                  │
        │  • 创作角度说明                          │
        │  • 传播潜力预测                          │
        └───────────────────────────────────────────────┘
                              ↓
                    ┌─────────────────────────┐
                    │  选择或忽略选题      │
                    └─────────────────────────┘
                              ↓
                    ┌─────────────────────────┐
                    │  [切换到内容创作]    │
                    └─────────────────────────┘
                              ↓

                    ┌─────────────────────────┐
                    │  模式B：内容创作       │
                    └─────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤1：选择内容类目                     │
        │  ┌───────────────────────────────────┐      │
        │  │ ○ 讲我的故事                  │      │
        │  │   • 创业经历                   │      │
        │  │   • 个人成长                   │      │
        │  │ ○ 展示我的业务                  │      │
        │  │   • 产品介绍                   │      │
        │  │   • 案例展示                   │      │
        │  │ ○ 解决客户问题                  │      │
        │  │ ○ 分享我的观点                  │      │
        │  │ ○ 拍一个系列                    │      │
        │  └───────────────────────────────────┘      │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤2：设置高级参数（可选）              │
        │  ────────────────────────────────────         │
        │  品牌调性：[专业、真诚、温暖]             │
        │  额外补充：[产品卖点、目标受众...]         │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤3：点击"一键生成全平台文案"          │
        │  ────────────────────────────────────         │
        │  ⚡ Dify Chatflow 智能生成           │
        │  • 自动注入战略上下文                    │
        │  • 深度洞察分析                         │
        │  • 全平台适配                           │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤4：流式输出生成内容                 │
        │  ────────────────────────────────────         │
        │  🔥 创意洞察分析                       │
        │  • IP 特点分析                         │
        │  • 用户痛点分析                         │
        │  • 产品卖点提炼                         │
        │                                       │
        │  📱 小红书文案                         │
        │  • 标题（3个爆款）                     │
        │  • 图文笔记（1000字）                   │
        │  • 推荐标签（5-8个）                   │
        │  • 配图建议（3-4张）                   │
        │                                       │
        │  🎬 抖音文案                           │
        │  • 黄金3秒标题                         │
        │  • 口播脚本（45秒）                     │
        │  • 分镜设计（3个镜头）                  │
        │  • BGM 建议                            │
        │                                       │
        │  📺 视频号文案                         │
        │  • 标题（2个优化）                     │
        │  • 视频脚本（5分钟）                   │
        │  • 分镜设计（8个镜头）                  │
        │  • 封面建议                            │
        │                                       │
        │  📝 公众号文案                         │
        │  • 标题（3个优化）                     │
        │  • 公众号文章（2000字）                 │
        │  • 关注+转发引导                       │
        │                                       │
        │  💬 朋友圈文案                         │
        │  • 短小精悍（3-5行）                   │
        │                                       │
        │  💡 优化建议                           │
        │  • 发布时间建议                         │
        │  • 互动引导策略                         │
        │  • 数据追踪建议                         │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤5：复制全文或新建创作               │
        │  • 复制到剪贴板                         │
        │  • 开始新的创作                         │
        └───────────────────────────────────────────────┘
                              ↓

                    ┌─────────────────────────┐
                    │  模式C：数据洞察       │
                    └─────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤1：上传CSV数据文件                 │
        │  [选择文件] 按钮                      │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤2：设置分析参数（可选）              │
        │  • 时间范围                            │
        │  • 平台选择                            │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤3：点击"开始分析"                  │
        │  → 模拟AI分析（待接入真实API）         │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤4：查看效果分析报告                 │
        │  • 爆款特征分析                         │
        │  • 核心数据透视                         │
        │  • AI 优化建议                          │
        └───────────────────────────────────────────────┘
                              ↓

                    ┌─────────────────────────┐
                    │  模式D：发布计划       │
                    └─────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤1：选择要发布的内容                 │
        │  • 从历史记录选择                       │
        │  • 导入新内容                          │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤2：设置发布时间                    │
        │  [日期时间选择器]                      │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤3：选择发布平台                    │
        │  • 小红书 ✓                            │
        │  • 抖音 ✓                              │
        │  • 视频号 ✓                            │
        │  • 公众号 ✓                            │
        └───────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  步骤4：确认发布计划                    │
        │  [📅 确认发布] 按钮                   │
        └───────────────────────────────────────────────┘
```

### 2.2 跨模式数据流

```
找爆款选题 → 选择选题 → 内容创作
    ↓              ↓           ↓
热点数据    选题标题    流式生成
    ↓              ↓           ↓
选题建议    战略上下文    全平台文案
                            ↓
                      数据洞察 ← 发布计划
                            ↓
                      效果分析    发布管理
```

---

## 三、页面框架图

### 3.1 三栏布局结构

```
┌──────────────────────────────────────────────────────────────────────────┐
│  神韵·内容板块 (Content Factory)                              │
│  路由：/content-factory                                       │
└──────────────────────────────────────────────────────────────────────────┘
                              │
        ┌───────────────────────────┬───────────────────────────┬───────────────────────────┐
        │                       │                       │
        ↓                       ↓                       ↓
┌───────────────┐       ┌───────────────┐       ┌───────────────┐
│  左栏         │       │  中栏         │       │  右栏         │
│  导航+历史    │       │  操作台       │       │  输出区       │
├───────────────┤       ├───────────────┤       ├───────────────┤
│ 宽度：70px    │       │ 宽度：384px  │       │ 宽度：自适应  │
│（展开时）     │       │（可折叠）     │       │（最小320px）   │
│              │       │               │       │               │
│ 📍 模式导航  │       │ 📍 操作区     │       │ 📍 AI 输出    │
│ • 找爆款      │       │ 根据模式动态   │       │ • Markdown     │
│ • 内容创作    │       │ 加载对应组件   │       │ • 流式显示     │
│ • 数据洞察    │       │               │       │ • 复制功能     │
│ • 发布计划    │       │ ┌───────────┐ │       │ • 新建功能     │
│              │       │ │搜索模式   │ │       │               │
│ 🕘 历史记录  │       │ └───────────┘ │       │ ┌───────────┐ │
│ • 上一次生成  │       │               │       │ │加载状态   │ │
│ • 更多历史    │       │ ┌───────────┐ │       │ └───────────┘ │
│              │       │ │创作模式   │ │       │               │
│ 🔧 底部操作  │       │ └───────────┘ │       │ ┌───────────┐ │
│ • 复制全文    │       │               │       │ │错误状态   │ │
│ • 展开/收起   │       │ ┌───────────┐ │       │ └───────────┘ │
│              │       │ │洞察模式   │ │       │               │
│ ─────────    │       │ └───────────┘ │       │ ┌───────────┐ │
│              │       │               │       │ │空状态     │ │
│              │       │ ┌───────────┐ │       │ └───────────┘ │
│              │       │ │计划模式   │ │       │               │
│              │       │ └───────────┘ │       │               │
│              │       │               │       │               │
│              │       │ 🎛️ 顶部工具栏  │       │ ┌───────────┐ │
│              │       │ • 专注阅读    │       │ │内容展示   │ │
│              │       │ • 复制全文    │       │ └───────────┘ │
│              │       │               │       │               │
└───────────────┘       └───────────────┘       └───────────────┘
```

### 3.2 响应式断点

```
桌面端（≥1024px）：
┌─────────────────────────────────────────────────────┐
│ 左栏(280px) │ 中栏(384px) │ 右栏(自适应)  │
└─────────────────────────────────────────────────────┘

平板端（768-1023px）：
┌─────────────────────────────────────────┐
│ 左栏(240px) │ 中栏+右栏（垂直排列）  │
└─────────────────────────────────────────┘

移动端（<768px）：
┌─────────────────────┐
│ 中栏（全屏）      │
│ 左右栏抽屉式     │
└─────────────────────┘
```

### 3.3 可折叠机制

```
左侧栏折叠：
┌───────────────┬───────────────────────────┬───────────────────────────┐
│ [≡]         │       中栏         │       右栏         │
│              │                       │               │
│ • 🔍        │                       │               │
│ • ✍️        │                       │               │
│ • 📊        │                       │               │
│ • 📅        │                       │               │
│              │                       │               │
└───────────────┴───────────────────────────┴───────────────────────────┘
  (70px展开)    (384px)              (自适应)

中栏折叠：
┌───────────────┬───────────────────────────┬───────────────────────────┐
│ 左栏         │       │               │       右栏         │
│              │ [▶ 展开编辑]  │                       │
│              │                       │               │
│ • 🔍        │                       │               │
│ • ✍️        │                       │               │
│ • 📊        │                       │               │
│ • 📅        │                       │               │
│              │                       │               │
└───────────────┴───────────────────────────┴───────────────────────────┘
  (280px)      (0px收起)              (自适应)

专注阅读模式：
┌───────────────┬───────────────────────────┬───────────────────────────┐
│ [≡]         │ [▶ 显示输入]  │       右栏（全屏）   │
│              │                       │                       │
│ • 🔍        │                       │               │
│ • ✍️        │                       │ 📍 AI 输出    │
│ • 📊        │                       │ • 完整内容     │
│ • 📅        │                       │ • 专注阅读     │
│              │                       │               │
└───────────────┴───────────────────────────┴───────────────────────────┘
  (70px)      (0px收起)              (100%宽度)
```

---

## 四、组件结构图

### 4.1 组件树

```
ContentFactoryPage (主页面)
│
├── ContentFactoryLayout (布局容器)
│   │
│   ├── 左侧栏
│   │   ├── 模式导航（4个按钮）
│   │   ├── 历史记录
│   │   └── 底部操作（复制按钮）
│   │
│   ├── 中栏（操作台）
│   │   ├── SearchModeInput (找爆款)
│   │   ├── CreateModeInput (内容创作)
│   │   ├── InsightsModeInput (数据洞察)
│   │   └── PlanModeInput (发布计划)
│   │
│   └── 右栏（输出区）
│       ├── 加载状态
│       ├── 错误状态
│       ├── 空状态
│       └── Markdown (流式输出)
│
├── 状态管理
│   ├── mode: 当前模式
│   ├── isStreaming: 是否正在生成
│   ├── generatedContent: 生成的内容
│   ├── historyContent: 历史内容
│   ├── error: 错误信息
│   ├── conversationId: 对话ID
│   └── strategyContext: 战略上下文
│
└── 数据流
    ├── fetchStrategyContext() (从Supabase获取）
    ├── streamFromApi() (SSE流式调用）
    ├── handleSearch() (搜索处理）
    ├── handleCreate() (创作处理）
    ├── handleAnalyzeInsights() (洞察处理）
    └── handleCopy() (复制处理）
```

### 4.2 SearchModeInput 组件

```
SearchModeInput
│
├── 状态
│   ├── keyword: 搜索关键词
│   ├── isFetching: 是否正在获取
│   ├── results: 搜索结果
│   ├── fetchError: 错误信息
│   └── selectedInsightId: 选中的选题ID
│
├── UI 元素
│   ├── 搜索输入框
│   │   ├── placeholder: "例如：AI、创业、内容营销..."
│   │   └── onKeyDown: Enter键触发
│   │
│   ├── 搜索按钮
│   │   ├── 文本: "搜索热点"
│   │   └── loading: spinner
│   │
│   ├── 数据来源说明
│   │   ├── 图标: rss_feed
│   │   └── 文本: "聚合全球 10+ 科技/AI 资讯网站..."
│   │
│   ├── 加载状态
│   │   ├── spinner
│   │   └── 文本: "正在检索全网热点..."
│   │
│   ├── 错误提示
│   │   ├── 图标: error_outline
│   │   └── 文本: 错误信息
│   │
│   ├── 热点文章列表
│   │   └── ArticleItem (可点击链接)
│   │       ├── 排名 (1-8)
│   │       ├── 标题
│   │       ├── 来源
│   │       ├── 时间
│   │       ├── 潜力分
│   │       └── 标签
│   │
│   └── AI 选题建议
│       └── InsightItem (可选中使用）
│           ├── 标题
│           ├── 描述
│           ├── 推荐理由
│           ├── 潜力分
│           ├── 来源链接
│           └── 使用按钮
│
└── 数据流
    ├── handleSearch() → GET /api/news-radar
    ├── onSelectInsight() → 传递给父组件
    └── onSearch() → 同步触发Dify分析
```

### 4.3 CreateModeInput 组件

```
CreateModeInput
│
├── 状态
│   ├── category: 内容类目
│   ├── subType: 子类型
│   ├── brandVoice: 品牌调性
│   ├── extraContext: 额外补充
│   └── showAdvanced: 是否展开高级参数
│
├── UI 元素
│   ├── 类目选择
│   │   ├── 讲我的故事 (personal-story)
│   │   │   ├── 创业经历
│   │   │   ├── 个人成长
│   │   │   └── 转型故事
│   │   ├── 展示我的业务 (business-showcase)
│   │   │   ├── 产品介绍
│   │   │   └── 案例展示
│   │   ├── 解决客户问题 (problem-solving)
│   │   ├── 分享我的观点 (opinion-sharing)
│   │   └── 拍一个系列 (themed-series)
│   │
│   ├── 子类型说明
│   │   ├── 💡 智能提示
│   │   └── 具体方向描述
│   │
│   ├── 高级参数（可折叠）
│   │   ├── 品牌调性输入框
│   │   │   └── placeholder: "专业、真诚、温暖"
│   │   ├── 额外补充输入框
│   │   │   └── placeholder: "产品卖点、用户痛点..."
│   │   └── 展开/收起按钮
│   │
│   ├── 深度引擎说明
│   │   ├── 🎯 图标
│   │   └── 文本: "AI 将基于你的 IP 特点..."
│   │
│   └── 生成按钮
│       ├── 图标: sparkles
│       ├── 文本: "一键生成全平台文案"
│       └── loading: spinner
│
└── 数据流
    ├── handleSubmit() → onSubmit(category, subType, identity, topic...)
    └── 展开/收起高级参数 → toggle showAdvanced
```

---

## 五、数据流图

### 5.1 找爆款选题数据流

```
用户操作
  ↓
输入关键词
  ↓
┌───────────────────────────────────────────┐
│  SearchModeInput 组件                 │
├───────────────────────────────────────────┤
│ handleSearch() 调用               │
└───────────────────────────────────────────┘
  ↓
┌───────────────────────────────────────────┐
│  /api/news-radar?keyword=xxx         │
├───────────────────────────────────────────┤
│ 1. 获取 ai-news-radar 数据              │
│    • URL: GitHub raw JSON            │
│    • 缓存: 30 分钟                   │
│ 2. 关键词过滤                         │
│    • 标题匹配                          │
│    • 标签匹配                          │
│ 3. 潜力打分                          │
│    • 关键词权重                       │
│    • 时效性评估                       │
│ 4. 选题生成                          │
│    • 模板映射                          │
│    • 潜力预测                          │
└───────────────────────────────────────────┘
  ↓
返回 JSON 响应
  ↓
┌───────────────────────────────────────────┐
│  前端展示                           │
├───────────────────────────────────────────┤
│ • 热点文章列表（可点击）            │
│ • AI 选题建议（可使用）               │
│ • 错误提示（如需要）                │
└───────────────────────────────────────────┘
  ↓
用户选择选题
  ↓
切换到"内容创作"模式
  ↓
传递选题标题到表单
```

### 5.2 内容创作数据流

```
用户操作
  ↓
填写表单（类目 + 子类型 + 高级参数）
  ↓
┌───────────────────────────────────────────┐
│  CreateModeInput 组件                 │
├───────────────────────────────────────────┤
│ handleSubmit() 调用                 │
└───────────────────────────────────────────┘
  ↓
┌───────────────────────────────────────────┐
│  ContentFactoryPage 父组件             │
├───────────────────────────────────────────┤
│ handleCreate() 注入战略上下文         │
└───────────────────────────────────────────┘
  ↓
┌───────────────────────────────────────────┐
│  /api/content-factory                 │
├───────────────────────────────────────────┤
│ 1. 接收请求参数                     │
│    • category: 类目                   │
│    • sub_type: 子类型                 │
│    • identity: 身份                   │
│    • topic: 主题                     │
│    • brand_voice: 品牌调性            │
│    • extra_context: 额外补充           │
│    • strategy_summary: 战略上下文          │
│ 2. 构造 Dify 请求                 │
│    • inputs: 所有参数                  │
│    • query: 主题文本                 │
│    • response_mode: "streaming"           │
│    • user: 用户ID                   │
│    • conversation_id: 对话ID（如有）       │
│ 3. 调用 Dify Chatflow              │
│    • URL: /chat-messages             │
│    • Method: POST                    │
│ 4. 处理 SSE 流                   │
│    • 解析 data 事件                  │
│    • 提取 answer 内容                │
│    • 追踪 conversation_id             │
│ 5. 返回 SSE 流                    │
└───────────────────────────────────────────┘
  ↓
┌───────────────────────────────────────────┐
│  Dify Chatflow                     │
├───────────────────────────────────────────┤
│ 1. 接收输入                       │
│    • 战略上下文注入                   │
│ 2. LLM 深度分析                   │
│    • IP 特点分析                     │
│    • 用户痛点分析                     │
│    • 产品卖点提炼                     │
│ 3. 多平台文案生成                   │
│    • 小红书适配                     │
│    • 抖音适配                        │
│    • 视频号适配                     │
│    • 公众号适配                     │
│    • 朋友圈适配                     │
│ 4. 优化建议生成                   │
│    • 发布时间建议                     │
│    • 互动引导策略                     │
│    • 数据追踪建议                     │
│ 5. 流式输出                       │
└───────────────────────────────────────────┘
  ↓
SSE 流式返回
  ↓
┌───────────────────────────────────────────┐
│  前端流式渲染                       │
├───────────────────────────────────────────┤
│ • 实时追加内容                       │
│ • Markdown 渲染                      │
│ • 自动滚动到最新                     │
│ • 专注阅读模式支持                   │
└───────────────────────────────────────────┘
  ↓
用户查看完整内容
  ↓
复制全文或新建创作
```

### 5.3 战略上下文注入

```
用户登录
  ↓
┌───────────────────────────────────────────┐
│  useEffect 自动获取                   │
├───────────────────────────────────────────┤
│ fetchStrategyContext()                │
└───────────────────────────────────────────┘
  ↓
┌───────────────────────────────────────────┐
│  Supabase 查询                     │
├───────────────────────────────────────────┤
│ SELECT * FROM strategy_contexts         │
│ WHERE user_id = current_user             │
│ ORDER BY created_at DESC              │
│ LIMIT 1                             │
└───────────────────────────────────────────┘
  ↓
返回战略上下文
  ↓
┌───────────────────────────────────────────┐
│  存储到状态                         │
├───────────────────────────────────────────┤
│ strategyContext: {                   │
│   niche: "美业培训",                │
│   revenueGoal: "月入10万",           │
│   founderStory: "从0到1的创业历程",    │
│   strengths: ["专业", "真诚", "温暖"], │
│   outputContent: "..."              │
│ }                                  │
└───────────────────────────────────────────┘
  ↓
用户点击"一键生成"
  ↓
┌───────────────────────────────────────────┐
│  handleCreate() 注入战略上下文          │
├───────────────────────────────────────────┤
│ const strategy_summary = JSON.stringify({   │
│   niche,                             │
│   revenue_goal,                       │
│   founder_story,                      │
│   strengths,                         │
│   strategic_output                     │
│ })                                  │
└───────────────────────────────────────────┘
  ↓
传递给 Dify
  ↓
Dify 工作流读取 strategy_summary
  ↓
基于战略上下文生成个性化内容
```

---

## 六、模式切换逻辑

### 6.1 状态机

```
初始状态: search
  │
  ├─→ 点击"找爆款选题"
  │    mode = "search"
  │    render: <SearchModeInput />
  │
  ├─→ 点击"内容创作"
  │    mode = "create"
  │    render: <CreateModeInput />
  │
  ├─→ 点击"数据洞察"
  │    mode = "insights"
  │    render: <InsightsModeInput />
  │
  └─→ 点击"发布计划"
       mode = "plan"
       render: <PlanModeInput />

搜索模式 (search)
  │
  ├─→ 用户选择选题
  │    mode = "create"
  │    topic = 选中的标题
  │    render: <CreateModeInput />
  │
  └─→ 点击其他模式按钮
       切换到对应模式

内容创作 (create)
  │
  ├─→ 点击"新建"
  │    清空 generatedContent
  │    清空 historyContent
  │    清空 conversationId
  │
  └─→ 点击其他模式按钮
       切换到对应模式
```

### 6.2 中栏组件渲染

```
renderInputComponent() {
  switch(mode) {
    case "search":
      return <SearchModeInput
        isLoading={isStreaming}
        onSearch={handleSearch}
        onSelectTopic={handleSelectTopic}
      />;

    case "create":
      return <CreateModeInput
        isLoading={isStreaming}
        onSubmit={handleCreate}
      />;

    case "insights":
      return <InsightsModeInput
        isLoading={isStreaming}
        onAnalyze={handleAnalyzeInsights}
      />;

    case "plan":
      return <PlanModeInput
        isLoading={isStreaming}
      />;

    default:
      return null;
  }
}
```

---

## 七、API调用图

### 7.1 API 端点

```
神韵·内容板块 (app/content-factory/)
│
├── page.tsx (主页面)
│   └── 状态管理 + 组件渲染
│
├── /api/content-factory/route.ts (Dify 代理)
│   └── POST /chat-messages (流式）
│
├── /api/news-radar/route.ts (热点数据代理)
│   └── GET latest-24h.json (GitHub）
│
└── components/
    ├── content-factory-layout.tsx
    ├── create-mode-input.tsx
    ├── search-mode-input.tsx
    ├── insights-mode-input.tsx
    └── plan-mode-input.tsx
```

### 7.2 Dify Chatflow 数据流

```
Next.js API Route
│
├── 1. 接收请求
│   POST /api/content-factory
│   Body: {
│     category,
│     sub_type,
│     identity,
│     topic,
│     brand_voice,
│     extra_context,
│     strategy_summary,
│     conversation_id
│   }
│
├── 2. 验证环境变量
│   DIFY_CONTENT_KEY = "app-xxxxxxxxx"
│
├── 3. 构造 Dify 请求
│   URL: https://api.dify.ai/v1/chat-messages
│   Headers: {
│     "Content-Type": "application/json",
│     "Authorization": `Bearer ${DIFY_CONTENT_KEY}`
│   }
│   Body: {
│     inputs: {
│       category,
│       sub_type,
│       identity,
│       brand_voice,
│       extra_context,
│       strategy_context: strategy_summary || "暂无战略上下文"
│     },
│     query: topic,
│     response_mode: "streaming",
│     user: "content-factory-user",
│     conversation_id: conversation_id || undefined
│   }
│
├── 4. 发送请求并处理 SSE 流
│   fetch(url, { method: "POST", body: JSON })
│   → ReadableStream
│   → reader.read()
│   → 解析 data 事件
│   → 提取 answer
│   → 提取 conversation_id
│
└── 5. 返回 SSE 流给前端
    Response(stream, {
      headers: {
        "Content-Type": "text/event-stream",
        "Cache-Control": "no-cache",
        "Connection": "keep-alive"
      }
    })
```

### 7.3 news-radar 数据流

```
Next.js API Route
│
├── 1. 接收请求
│   GET /api/news-radar?keyword=xxx&limit=8
│
├── 2. 获取 ai-news-radar 数据
│   URL: https://raw.githubusercontent.com/LearnPrompt/ai-news-radar/main/data/latest-24h.json
│   Headers: {
│     "User-Agent": "Mozilla/5.0 (ai-business-growth-os/1.0)"
│   }
│   Next.js 缓存: 30 分钟
│
├── 3. 过滤和打分
│   filtered = items.filter(item => {
│     text = item.title + item.title_zh + item.tags.join(" ")
│     return text.includes(keyword.toLowerCase())
│   })
│   scored = filtered.map(item => ({
│     ...item,
│     score: calcPotentialScore(item, keyword)
│   })).sort((a, b) => b.score - a.score).slice(0, limit)
│
├── 4. 生成选题建议
│   insights = scored.slice(0, 5).map((item, i) => ({
│     id: String(i + 1),
│     title: generateTitle(keyword, i),
│     description: getDescription(i),
│     reason: getReason(i),
│     potential_score: item.score,
│     source_url: item.url,
│     source_title: item.title_zh || item.title
│   }))
│
└── 5. 返回 JSON 响应
    Response.json({
      keyword,
      total: allItems.length,
      filtered: scored.length,
      generated_at: rawData.generated_at,
      articles: scored,
      insights
    })
```

---

## 八、错误处理流程

### 8.1 错误类型

```
┌─────────────────────────────────────────────────────────────────┐
│  错误分类                                            │
├─────────────────────────────────────────────────────────────────┤
│                                                        │
│  网络错误                                              │
│  ├─ 请求超时 (TimeoutError)                          │
│  ├─ 连接失败 (NetworkError)                          │
│  └─ API 不可达 (502/503)                           │
│                                                        │
│  API 错误                                               │
│  ├─ 400: 请求参数错误                                 │
│  ├─ 401: 未授权（API Key 错误）                   │
│  ├─ 403: 禁止访问（Bocha AI Key 错误）         │
│  ├─ 429: 请求过频                                     │
│  └─ 500: 服务器内部错误                               │
│                                                        │
│  数据错误                                               │
│  ├─ JSON 解析失败                                    │
│  ├─ 数据格式错误                                      │
│  └─ 缺少必需字段                                     │
│                                                        │
│  用户错误                                               │
│  ├─ 未输入关键词                                       │
│  ├─ 未选择类目                                       │
│  └─ 表单验证失败                                     │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 错误处理流程

```
用户操作
  ↓
触发 API 调用
  ↓
┌───────────────────────────────────────────┐
│  捕获异常                           │
├───────────────────────────────────────────┤
│ try {                               │
│   const resp = await fetch(...);     │
│   // 处理响应                       │
│ } catch (err) {                     │
│   // 错误处理                       │
│ }                                  │
└───────────────────────────────────────────┘
  ↓
┌───────────────────────────────────────────┐
│  错误分类                           │
├───────────────────────────────────────────┤
│ if (err instanceof TimeoutError) {    │
│   // 网络超时                       │
│ } else if (resp.status === 401) {   │
│   // API Key 错误                  │
│ } else if (resp.status === 502) {   │
│   // 网络错误                       │
│ } else {                             │
│   // 其他错误                       │
│ }                                  │
└───────────────────────────────────────────┘
  ↓
┌───────────────────────────────────────────┐
│  友好提示                           │
├───────────────────────────────────────────┤
│ ❌ 技术错误：                       │
│ "TimeoutError: Request timeout..."    │
│                                      │
│ ✅ 用户友好：                         │
│ "服务响应时间过长，请稍后再试。"    │
└───────────────────────────────────────────┘
  ↓
┌───────────────────────────────────────────┐
│  UI 反馈                           │
├───────────────────────────────────────────┤
│ • 显示错误提示框                    │
│ • 红色图标 + 描述                   │
│ • 恢复按钮（如需要）                │
│ • 不显示技术细节                   │
└───────────────────────────────────────────┘
  ↓
用户可以重试或修改操作
```

---

## 九、性能优化

### 9.1 缓存策略

```
ai-news-radar 数据缓存：
┌───────────────────────────────────────────┐
│  Next.js 缓存配置                   │
├───────────────────────────────────────────┤
│ next: {                             │
│   revalidate: 1800  // 30 分钟       │
│ }                                  │
└───────────────────────────────────────────┘
  ↓
优势：
  ✅ 减少 GitHub API 调用
  ✅ 提升响应速度
  ✅ 降低带宽消耗

战略上下文缓存：
┌───────────────────────────────────────────┐
│  React 状态缓存                     │
├───────────────────────────────────────────┤
│ useEffect 只在组件挂载时执行          │
│ 不重复查询 Supabase                  │
└───────────────────────────────────────────┘
```

### 9.2 流式输出优化

```
SSE 流式处理：
┌───────────────────────────────────────────┐
│  批量处理                           │
├───────────────────────────────────────────┤
│ const lines = buffer.split("\n\n");   │
│ for (const line of lines) {           │
│   const data = parseDataLine(line);  │
│   if (data) {                       │
│     appendContent(data.content);        │
│     // 批量更新 DOM                 │
│   }                                  │
│ }                                  │
└───────────────────────────────────────────┘
  ↓
优势：
  ✅ 减少 DOM 操作次数
  ✅ 提升渲染性能
  ✅ 避免页面卡顿
```

---

## 十、一句话总结

```
神韵·内容板块 = 三栏布局 + 四种模式 + 流式输出 + 智能生成

极简设计 + 实时反馈 + 友好错误 = 优秀用户体验。
```

---

> 这是产品的"交互与智能"
> 从界面到体验，从工具到伙伴
>
> ——— 2026年3月4日
> ——— 版本：v1.0（完整图表版）
